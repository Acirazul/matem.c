#include <stab.h>

int main(){
float x = 5.5;
float y = 2.2;
float soma = x + y;
float diferenca = x -Y;
float quociente = x / y; // Divisão de pontos flutuantes 

prinf("Soma: %.2f/n", soma);
prinf("Diferença: %.2f/n", diferenca);
prinf("Quociente: %.2f/n", quociente);

return 0;

}